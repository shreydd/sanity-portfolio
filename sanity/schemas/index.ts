import page from "./page-schema";
import project from "./project-schemas";
import skills from "./skills-schema";
import writing from "./writings-schema";

const schemas = [project, page, writing, skills];

export default schemas;